
import Layout from "@/components/Layout";
import CelebrityEmbeddingGenerator from "@/components/CelebrityEmbeddingGenerator";

const EmbeddingGenerator = () => {
  return (
    <Layout>
      <CelebrityEmbeddingGenerator />
    </Layout>
  );
};

export default EmbeddingGenerator;
